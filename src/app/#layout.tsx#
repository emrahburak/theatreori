import type { Metadata } from "next";
import "./globals.css";
import Nav from "@/components/Nav";
import bg from "@/assets/images/bg-01.jpg"


export const metadata: Metadata = {
	title: "Tiyatro Ori",
};


export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) {
	return (
		<html lang="en">
			<body
				className="relative bg-cover bg-center min-h-screen"
				style={{
					backgroundImage: `url(${bg.src})`,
				}}
			>
				{/* Gradient overlay */}
				<div className="absolute inset-0 bg-gradient-to-b from-black via-transparent to-white opacity-80  z-0"></div>

				{/* İçerik */}
				<header className="relative z-10 bg-slate">
					<Nav />
				</header>

				<main className="relative z-10 max-w-screen-xl mx-auto">
					{children}
				</main>

				<footer className="relative z-10">
					Footer
				</footer>
			</body>
		</html>
	);
}
